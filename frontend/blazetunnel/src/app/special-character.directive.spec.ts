import { SpecialCharacterDirective } from './special-character.directive';

describe('SpecialCharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialCharacterDirective();
    expect(directive).toBeTruthy();
  });
});
